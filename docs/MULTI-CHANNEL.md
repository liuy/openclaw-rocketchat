[首页](../README.md) · [使用指南](GUIDE.md) · [常见问题](FAQ.md) · [配置参数](CONFIGURATION.md) · [架构](ARCHITECTURE.md) · [安全](SECURITY.md) · [多 Agent](MULTI-AGENT.md) · [多渠道](MULTI-CHANNEL.md) · [命令](COMMANDS.md)

# 多渠道共存指南

## 为什么要多渠道共存？

你可能已经在用 Telegram 或其他平台和 OpenClaw 对话了。安装 `openclaw-rocketchat` 插件后，**不需要放弃现有渠道** —— 两者可以同时工作，互不干扰。

这意味着：
- **已有的 Telegram / Slack / WhatsApp 配置完全不受影响**
- **同一个 Agent 可以同时在多个平台上回复消息**
- 你可以先体验 Rocket.Chat 的优势，再决定长期方案

> **适合谁？** 已经配好了 Telegram/Slack 等渠道，想同时试试 Rocket.Chat 的用户。
> 如果你是全新安装，直接看 [使用指南](GUIDE.md) 即可。

---

## 核心原理

OpenClaw 的 Gateway 天然支持同时连接多个消息平台。每个平台在配置中是一个独立的 `channel`，互相隔离：

```
                    ┌─ channels.telegram ─── Telegram Bot
                    │
OpenClaw Gateway ───┼─ channels.rocketchat ─ Rocket.Chat（本插件）
                    │
                    └─ channels.slack ────── Slack Bot
```

**路由规则：**
- 消息从哪个平台来，回复就发回哪个平台
- 模型不需要"选择"平台，路由完全由配置决定
- 每条消息通过 `bindings` 规则匹配到对应的 Agent

---

## 实战：Telegram + Rocket.Chat 共存

这是最常见的场景 —— 你已经在用 Telegram，现在想加上 Rocket.Chat 给家人或团队用。

### 前提

- OpenClaw 已安装并正常运行
- Telegram Bot 已配好（`channels.telegram` 正常工作）
- 有一台公网服务器（用于部署 Rocket.Chat）

### 第一步：安装 Rocket.Chat + 插件

```bash
# 在服务器上一键安装 Rocket.Chat
curl -fsSL https://raw.githubusercontent.com/Kxiandaoyan/openclaw-rocketchat/master/install-rc.sh | bash

# 安装插件
openclaw plugins install openclaw-rocketchat

# 运行配置向导
openclaw rocketchat setup
```

**setup 向导不会动你现有的 Telegram 配置。** 它只写入 `channels.rocketchat` 部分。

### 第二步：添加机器人

```bash
openclaw rocketchat add-bot
# 用户名: molty
# 昵称: 小龙虾
# 绑定 Agent: main   ← 和 Telegram 用同一个 Agent
```

### 第三步：重启 Gateway

```bash
openclaw gateway stop && openclaw gateway start
```

**完成。** 现在你的 OpenClaw 同时在 Telegram 和 Rocket.Chat 上工作。

### 最终配置结构

你的 `openclaw.json` 会同时包含两个渠道：

```json5
{
  // Telegram 渠道（你原有的，不变）
  "channels": {
    "telegram": {
      "enabled": true,
      "botToken": "123:your_telegram_bot_token",
      "dmPolicy": "pairing",
      "groups": {
        "*": { "requireMention": true }
      }
    },

    // Rocket.Chat 渠道（新增的）
    "rocketchat": {
      "enabled": true,
      "serverUrl": "https://127.0.0.1",
      "port": 443,
      "dmPolicy": "pairing",
      "accounts": {
        "molty": {
          "botUsername": "molty",
          "botDisplayName": "小龙虾"
        }
      }
    }
  },

  // 路由规则（两条 binding，指向同一个 Agent）
  "bindings": [
    {
      "agentId": "main",
      "match": { "channel": "telegram" }
    },
    {
      "agentId": "main",
      "match": { "channel": "rocketchat", "accountId": "molty" }
    }
  ]
}
```

### 消息流向

```
你在 Telegram 发消息 → telegram channel → main Agent → 回复到 Telegram
家人在 Rocket.Chat 发消息 → rocketchat channel → main Agent → 回复到 Rocket.Chat
```

两边独立会话，互不干扰。Agent 相同，但每个平台的对话上下文是隔离的。

---

## 进阶：不同平台用不同 Agent

如果你想让 Telegram 和 Rocket.Chat 由不同的 AI「人格」来服务：

### 创建额外 Agent

```bash
openclaw agents add home    # 家庭助理，给 Rocket.Chat 用
```

### 创建机器人时选择新 Agent

```bash
openclaw rocketchat add-bot
# 用户名: home-bot
# 昵称: 家庭助手
# 绑定 Agent: home   ← 选不同的 Agent
```

### 最终效果

```json5
{
  "bindings": [
    // Telegram → main Agent（你自己用的私人助理）
    {
      "agentId": "main",
      "match": { "channel": "telegram" }
    },
    // Rocket.Chat → home Agent（家人用的家庭助手）
    {
      "agentId": "home",
      "match": { "channel": "rocketchat", "accountId": "home-bot" }
    }
  ]
}
```

| 平台 | Agent | 适用人群 | 模型 |
|------|-------|---------|------|
| Telegram | main | 你自己 | Claude / GPT（你原有的） |
| Rocket.Chat | home | 全家人 | GLM / 任意国产模型 |

> **更多多 Agent 玩法**请参阅 [多 Agent 指南](MULTI-AGENT.md)。

---

## 其他渠道组合

同样的方式适用于任何 OpenClaw 支持的渠道组合：

### Slack + Rocket.Chat

```json5
{
  "channels": {
    "slack": {
      "enabled": true,
      "mode": "socket",
      "appToken": "xapp-...",
      "botToken": "xoxb-..."
    },
    "rocketchat": {
      "enabled": true,
      "serverUrl": "https://127.0.0.1",
      "port": 443,
      // ...
    }
  }
}
```

### WhatsApp + Rocket.Chat

```json5
{
  "channels": {
    "whatsapp": {
      "enabled": true,
      "accounts": {
        "personal": {}
      }
    },
    "rocketchat": {
      "enabled": true,
      "serverUrl": "https://127.0.0.1",
      "port": 443,
      // ...
    }
  }
}
```

### 三渠道同时在线

```json5
{
  "channels": {
    "telegram": { "enabled": true, /* ... */ },
    "slack": { "enabled": true, /* ... */ },
    "rocketchat": { "enabled": true, /* ... */ }
  },
  "bindings": [
    { "agentId": "main", "match": { "channel": "telegram" } },
    { "agentId": "work", "match": { "channel": "slack", "teamId": "T123456789" } },
    { "agentId": "home", "match": { "channel": "rocketchat", "accountId": "molty" } }
  ]
}
```

---

## Rocket.Chat 的独特优势

在多渠道共存的场景下，Rocket.Chat 有一些其他平台不具备的特点：

| 特性 | Telegram | Slack | Rocket.Chat |
|------|:---:|:---:|:---:|
| 中国大陆直连 | ❌ 需 VPN | ✅ | ✅ |
| 数据完全自控 | ❌ 经 TG 服务器 | ❌ 经 Slack 服务器 | ✅ 本地存储 |
| 多人共享同一套服务 | ❌ 各用各的 | 需企业版 | ✅ 开箱即用 |
| 只读权限（旁听/审计） | ❌ | 有限 | ✅ 原生支持 |
| 多机器人群组 + @提及 | ✅ | ✅ | ✅ |
| 独立通知推送 | ❌ 混在社交中 | ❌ 混在工作消息中 | ✅ 专属 App |
| 免费自托管 | ❌ | ❌ | ✅ |

**典型的迁移路径：**

1. 先保持 Telegram 不动，加上 Rocket.Chat
2. 把家人/同事拉到 Rocket.Chat（不需要 VPN，不需要注册第三方账号）
3. 体验一段时间后，根据实际需求决定长期方案

---

## 架构图：多渠道共存

```
┌────────────────┐  ┌────────────────┐  ┌────────────────┐
│  📱 Telegram   │  │  💬 Slack      │  │  🚀 Rocket.Chat│
│  (你自己用)     │  │  (工作团队)     │  │  (家人/团队)    │
└───────┬────────┘  └───────┬────────┘  └───────┬────────┘
        │                   │                    │
        │ Bot API           │ Socket Mode        │ REST + DDP
        │                   │                    │
┌───────▼───────────────────▼────────────────────▼────────┐
│                    OpenClaw Gateway                       │
│                                                          │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────────┐ │
│  │ telegram     │ │ slack        │ │ rocketchat        │ │
│  │ channel      │ │ channel      │ │ channel (本插件)   │ │
│  └──────┬───────┘ └──────┬───────┘ └────────┬─────────┘ │
│         │                │                   │           │
│         ▼                ▼                   ▼           │
│  ┌──────────────────────────────────────────────────┐   │
│  │              bindings 路由规则                     │   │
│  └──────┬──────────────┬─────────────────┬──────────┘   │
│         ▼              ▼                 ▼               │
│  ┌──────────┐   ┌──────────┐      ┌──────────┐         │
│  │  Agent:  │   │  Agent:  │      │  Agent:  │         │
│  │  main    │   │  work    │      │  home    │         │
│  └──────────┘   └──────────┘      └──────────┘         │
└──────────────────────────────────────────────────────────┘
```

---

## 常见问题

### Q: 安装 Rocket.Chat 插件会影响我现有的 Telegram 配置吗？

不会。`openclaw rocketchat setup` 只操作 `channels.rocketchat` 部分，不会修改 `channels.telegram` 或其他渠道的配置。

### Q: 同一个 Agent 同时服务两个平台，上下文会串吗？

不会。每个平台的每个会话（DM / 群组）都有独立的 session，上下文完全隔离。在 Telegram 聊的内容不会出现在 Rocket.Chat 的对话中。

### Q: 我可以先试用 Rocket.Chat，不满意再卸载吗？

可以。运行 `openclaw rocketchat uninstall` 即可完全卸载，不影响其他渠道。

### Q: 多个渠道同时运行会增加资源消耗吗？

Gateway 本身的资源消耗很小。主要消耗来自 AI 模型的 API 调用，这跟消息数量相关，与渠道数量无关。Rocket.Chat 容器额外需要约 512MB-1GB 内存。

### Q: 不同渠道可以用不同的 AI 模型吗？

可以。通过创建不同的 Agent 并绑定到不同渠道，每个 Agent 可以配置不同的模型。例如 Telegram 用 Claude，Rocket.Chat 用 GLM。参见 [多 Agent 指南](MULTI-AGENT.md)。

### Q: 升级 Rocket.Chat 插件会影响 Telegram 吗？

不会。`openclaw rocketchat upgrade` 只更新本插件，升级过程中会短暂停止 Gateway（通常几秒），但重启后所有渠道都会恢复工作。
